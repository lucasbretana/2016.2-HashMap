// Erro code
#define SUCCESS 0
#define INVALID_PARAMETER 1

int length(char *k); // uses the default, '\0' to be ignored
int lngign(char *, char);
int strcomp(char *, const char *);
long long int powlld(long long int, long long int);
